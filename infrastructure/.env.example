# CDK deploy — copy to .env: cp .env.example .env
# Deploy KaiznBonsaiFrontendStack first, then set CORS_ALLOWED_ORIGINS to its CloudFrontURL.

DJANGO_SECRET_KEY=
DB_PASSWORD=

POSTGRES_USER=kaiznbonsai
POSTGRES_DB=kaiznbonsaidb
